---
import Layout from '../../layouts/Layout.astro';
import { getCollection } from 'astro:content';
import { PILIERS } from '../../content.config';

const base = import.meta.env.BASE_URL;

const posts = (await getCollection('blog', ({ data }) => !data.draft))
  .sort((a, b) => b.data.publishDate.getTime() - a.data.publishDate.getTime());

const featured = posts.find((p) => p.data.featured);
const others = posts.filter((p) => p !== featured);

const formatMonthYear = (d: Date) =>
  d.toLocaleDateString('fr-CA', { year: 'numeric', month: 'long' })
    .replace(/^./, (c) => c.toUpperCase());
---
<Layout
  title="Le journal du village — Parent un jour"
  description="Le journal de Parent un jour — articles sourcés sur la parentalité, écrits avec soin. Pour les 3 h du matin comme pour les dimanches tranquilles."
>

  <!-- ===== HEADER JOURNAL ===== -->
  <header class="journal-header">

    <!-- Motif botanique inspiré du logo -->
    <svg class="botanical" viewBox="0 0 120 240" fill="none" xmlns="http://www.w3.org/2000/svg" aria-hidden="true">
      <path d="M 60 240 C 58 210, 50 185, 48 160 C 46 135, 50 110, 55 85 C 60 60, 65 40, 62 15"
            stroke="#83967D" stroke-width="1.8" stroke-linecap="round" fill="none" opacity="0.7"/>
      <path d="M 55 240 C 53 215, 42 190, 40 168 C 38 145, 42 125, 48 100 C 54 75, 58 50, 55 25"
            stroke="#4C5861" stroke-width="1.2" stroke-linecap="round" fill="none" opacity="0.35"/>
      <path d="M 48 165 C 30 160, 16 150, 12 140 C 22 137, 36 145, 46 158" fill="#83967D" opacity="0.3"/>
      <path d="M 50 120 C 32 116, 18 106, 15 96 C 24 93, 38 102, 48 114" fill="#83967D" opacity="0.25"/>
      <path d="M 55 78 C 40 73, 28 63, 26 54 C 34 52, 46 60, 54 72" fill="#83967D" opacity="0.2"/>
      <path d="M 50 142 C 68 138, 82 128, 86 118 C 76 115, 62 124, 52 136" fill="#83967D" opacity="0.25"/>
      <path d="M 53 98 C 70 93, 84 84, 87 74 C 78 72, 64 80, 54 92" fill="#83967D" opacity="0.2"/>
      <circle cx="48" cy="160" r="5.5" fill="#A8B9A3" opacity="0.5"/>
      <circle cx="50" cy="118" r="5" fill="#A8B9A3" opacity="0.45"/>
      <circle cx="55" cy="76" r="4.5" fill="#A8B9A3" opacity="0.4"/>
      <circle cx="58" cy="42" r="3.5" fill="#A8B9A3" opacity="0.35"/>
      <circle cx="52" cy="138" r="3" fill="#D9E1D4" opacity="0.55"/>
      <circle cx="54" cy="96" r="3" fill="#D9E1D4" opacity="0.5"/>
      <circle cx="57" cy="58" r="2.5" fill="#D9E1D4" opacity="0.4"/>
    </svg>

    <div class="inner">
      <div class="top-row">
        <h1>Le journal du village</h1>
        <span class="cursive-tag">lire, comprendre, se retrouver</span>
      </div>
      <p class="subtitle">
        Des articles sourcés sur la parentalité, écrits avec soin.
        Pour les 3&nbsp;h du matin comme pour les dimanches tranquilles.
      </p>
    </div>
  </header>

  <!-- ===== FILTERS BAR ===== -->
  <div class="filters-bar" role="tablist" aria-label="Filtrer les articles par pilier">
    <button type="button" class="filter-pill active" data-filter="all" role="tab" aria-selected="true">Tous</button>
    {PILIERS.map((pilier) => (
      <button type="button" class="filter-pill" data-filter={pilier} role="tab" aria-selected="false">{pilier}</button>
    ))}
    <button type="button" class="search-btn" aria-label="Rechercher (bientôt disponible)" title="Recherche — bientôt disponible" disabled>
      <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
        <circle cx="11" cy="11" r="7"></circle>
        <line x1="21" y1="21" x2="16.65" y2="16.65"></line>
      </svg>
    </button>
  </div>

  <!-- ===== ARTICLES GRID ===== -->
  <div class="articles-grid">

    {featured && (
      <a
        href={`${base}le-journal/${featured.id}/`}
        class="article-card featured"
        data-piliers={featured.data.piliers.join('|')}
      >
        <div class="card-img">{featured.data.thumbnailEmoji}</div>
        <div class="card-body">
          <div class="card-pilier">Article à la une</div>
          <div class="card-title">{featured.data.title}</div>
          <div class="card-excerpt">{featured.data.description}</div>
          <div class="card-meta">
            Par {featured.data.author} · {formatMonthYear(featured.data.publishDate)}
            {featured.data.readingTime && <> · {featured.data.readingTime}</>}
          </div>
        </div>
      </a>
    )}

    {others.map((post) => (
      <a
        href={`${base}le-journal/${post.id}/`}
        class="article-card"
        data-piliers={post.data.piliers.join('|')}
      >
        <div class="card-img">{post.data.thumbnailEmoji}</div>
        <div class="card-body">
          <div class="card-pilier">{post.data.piliers[0]}</div>
          <div class="card-title">{post.data.title}</div>
          <div class="card-excerpt">{post.data.description}</div>
          <div class="card-meta">
            {post.data.readingTime ? `${post.data.readingTime} de lecture` : formatMonthYear(post.data.publishDate)}
          </div>
        </div>
      </a>
    ))}

    {posts.length === 0 && (
      <p class="journal-empty">Aucun article publié pour le moment — les premiers arrivent bientôt.</p>
    )}
    <p class="journal-empty" id="journal-no-results" hidden>
      Aucun article pour ce pilier pour le moment.
    </p>
  </div>

  <!-- ===== CTA ===== -->
  <section class="cta-section">
    <div class="container">
      <h2>Ne manque rien du journal</h2>
      <div class="script">un article à la fois.</div>
      <p>
        Suis Parent un jour sur Instagram pour être averti·e des nouveaux articles —
        et rejoins une communauté de parents qui osent parler vrai.
      </p>
      <div class="cta-buttons">
        <a href="https://www.instagram.com/parentunjour" class="btn btn-primary" target="_blank" rel="noopener">Me suivre sur Instagram</a>
        <a href={`${base}rss.xml`} class="btn btn-outline">Flux RSS</a>
      </div>
    </div>
  </section>

</Layout>

<script is:inline>
  (() => {
    const noResults = document.getElementById('journal-no-results');
    const pills = document.querySelectorAll('.filter-pill');
    const cards = document.querySelectorAll('.articles-grid .article-card');

    pills.forEach((pill) => {
      pill.addEventListener('click', () => {
        const filter = pill.dataset.filter;

        pills.forEach((p) => {
          p.classList.toggle('active', p === pill);
          p.setAttribute('aria-selected', p === pill ? 'true' : 'false');
        });

        let visible = 0;
        cards.forEach((card) => {
          const piliers = (card.dataset.piliers || '').split('|');
          const match = filter === 'all' || piliers.includes(filter);
          card.style.display = match ? '' : 'none';
          if (match) visible++;
        });

        if (noResults) noResults.hidden = filter === 'all' || visible > 0;
      });
    });
  })();
</script>

<style>
  /* ===== HEADER JOURNAL ===== */
  .journal-header {
    padding: 180px 40px 100px;
    background: var(--cream);
    border-bottom: 3px solid var(--sage);
    position: relative;
    overflow: hidden;
  }
  .journal-header .botanical {
    position: absolute;
    bottom: -10px;
    left: 40px;
    width: 120px;
    height: auto;
    opacity: 0.55;
    pointer-events: none;
  }
  .journal-header .inner {
    max-width: 960px;
    margin: 0 auto;
    position: relative;
    z-index: 1;
  }
  .journal-header .top-row {
    display: flex;
    align-items: baseline;
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 6px;
  }
  .journal-header h1 {
    font-family: 'Georgia', serif;
    font-size: clamp(26px, 3.5vw, 36px);
    font-weight: 400;
    color: var(--slate);
    line-height: 1.2;
  }
  .journal-header .cursive-tag {
    font-family: 'La Belle Aurore', cursive;
    color: var(--terra);
    font-size: 22px;
    flex-shrink: 0;
  }
  .journal-header .subtitle {
    font-size: 15px;
    color: var(--slate);
    opacity: 0.85;
    max-width: 560px;
    line-height: 1.6;
    margin-top: 2px;
  }

  /* ===== FILTERS BAR ===== */
  .filters-bar {
    max-width: 960px;
    margin: 0 auto;
    padding: 48px 40px 48px;
    display: flex;
    align-items: center;
    justify-content: center;
    gap: 10px;
    flex-wrap: wrap;
  }
  .filter-pill {
    font-family: 'Lato', sans-serif;
    font-size: 12.5px;
    font-weight: 400;
    padding: 7px 16px;
    border-radius: 999px;
    border: 1px solid var(--sage-soft);
    color: var(--slate);
    opacity: 0.85;
    background: transparent;
    cursor: pointer;
    transition: all 0.25s;
    letter-spacing: 0.2px;
  }
  .filter-pill:hover {
    border-color: var(--sage);
    color: var(--slate);
    opacity: 1;
    background: var(--sage-light);
  }
  .filter-pill.active {
    background: var(--sage);
    color: var(--white);
    border-color: var(--sage);
    opacity: 1;
  }
  .search-btn {
    width: 36px; height: 36px;
    border-radius: 50%;
    border: 1px solid var(--sage-soft);
    background: transparent;
    display: flex; align-items: center; justify-content: center;
    cursor: not-allowed;
    margin-left: auto;
    opacity: 0.6;
    color: var(--slate);
  }
  .search-btn svg {
    width: 15px; height: 15px;
  }

  /* ===== ARTICLES GRID ===== */
  .articles-grid {
    max-width: 960px;
    margin: 0 auto;
    padding: 32px 40px 96px;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 24px;
  }
  .articles-grid .article-card {
    background: var(--white);
    border-radius: 12px;
    overflow: hidden;
    box-shadow: 0 2px 20px rgba(76, 88, 97, 0.06);
    border: 1px solid rgba(168, 185, 163, 0.15);
    transition: transform 0.3s, box-shadow 0.3s;
    text-decoration: none;
    color: inherit;
    display: flex;
    flex-direction: column;
  }
  .articles-grid .article-card:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 30px rgba(76, 88, 97, 0.1);
  }
  .articles-grid .card-img {
    height: 160px;
    background: linear-gradient(135deg, var(--sage-light), var(--terra-light));
    display: flex;
    align-items: center;
    justify-content: center;
    font-size: 40px;
  }
  .articles-grid .card-body {
    padding: 20px;
    flex: 1;
    display: flex;
    flex-direction: column;
  }
  .articles-grid .card-pilier {
    font-size: 11px;
    letter-spacing: 2px;
    text-transform: uppercase;
    color: var(--terra);
    font-weight: 700;
    margin-bottom: 8px;
  }
  .articles-grid .card-title {
    font-family: 'Georgia', serif;
    font-size: 17px;
    font-weight: 400;
    color: var(--slate);
    line-height: 1.35;
    margin-bottom: 8px;
  }
  .articles-grid .card-excerpt {
    font-size: 14px;
    color: var(--slate);
    opacity: 0.8;
    line-height: 1.55;
    flex: 1;
  }
  .articles-grid .card-meta {
    font-size: 12px;
    color: var(--sage);
    margin-top: 14px;
    font-style: italic;
  }

  /* Featured (pleine largeur) */
  .articles-grid .article-card.featured {
    grid-column: 1 / -1;
    display: grid;
    grid-template-columns: 1.2fr 1fr;
    flex-direction: initial;
  }
  .articles-grid .article-card.featured .card-img {
    height: 100%;
    min-height: 220px;
    font-size: 72px;
  }
  .articles-grid .article-card.featured .card-body {
    padding: 36px;
    justify-content: center;
  }
  .articles-grid .article-card.featured .card-title {
    font-size: 24px;
  }
  .articles-grid .article-card.featured .card-excerpt {
    font-size: 15px;
    line-height: 1.6;
  }

  .journal-empty {
    grid-column: 1 / -1;
    text-align: center;
    font-style: italic;
    color: var(--sage);
    padding: 60px 20px;
  }

  @media (max-width: 900px) {
    .articles-grid { grid-template-columns: repeat(2, 1fr); }
  }

  @media (max-width: 700px) {
    .journal-header { padding: 140px 20px 72px; }
    .journal-header .botanical { width: 70px; opacity: 0.35; left: 12px; }
    .journal-header .top-row {
      flex-direction: column;
      align-items: flex-start;
      gap: 4px;
    }
    .filters-bar { padding: 24px 20px 16px; }
    .filter-pill { font-size: 12px; padding: 6px 14px; }
    .search-btn { margin-left: 0; margin-top: 4px; }
    .articles-grid {
      grid-template-columns: 1fr;
      padding: 24px 20px 72px;
      gap: 20px;
    }
    .articles-grid .article-card.featured {
      grid-template-columns: 1fr;
    }
    .articles-grid .article-card.featured .card-img {
      min-height: 160px;
      font-size: 56px;
    }
    .articles-grid .article-card.featured .card-body { padding: 24px; }
    .articles-grid .article-card.featured .card-title { font-size: 20px; }
  }
</style>
